# Zero-Shot测试配置

# 模型配置
model:
  size: big
  resume: /mnt/huangjiaxin/3d-defense/LGM/pretrained/model_fp16_fixrot.safetensors
  device: cuda
  gpu_ids: "7"

# 数据配置
data:
  root: /mnt/huangjiaxin/3d-defense/datas
  categories: null  # null表示使用所有类别
  max_samples: 50  # 测试50个样本
  num_workers: 4
  view_selector: orthogonal  # 使用标准正交视图
  angle_offset: 0.0
  num_supervision_views: 4  # 限制监督视图数量，避免OOM

# 其他配置
misc:
  workspace: ./workspace_zero_shot
  seed: 42
